Tags: report, synthesis, summary, 报告

# Report Synthesis Domain Pack

- Separate **findings**, **evidence**, and **limitations** sections.
- Each claim must map to a tool evidence object with `source` and `retrieved_at`.
- Use `build_report.py` with `inputs/evidence.json` for structured output.
- State uncertainty when evidence is conflicting or incomplete.
