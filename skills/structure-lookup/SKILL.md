# Structure Lookup

## Experimental structures
Use `bio_pdb_search` against RCSB PDB (rcsb.org search API).

## Predicted structures
When UniProt accession is known, use `bio_alphafold_lookup` (AlphaFold DB, EBI).
