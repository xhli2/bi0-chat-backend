# General Assistant Utilities

1. Use `time_now` for current date/time questions (UTC ISO by default).
2. Use `summarize_chunk` to shorten long pasted text.
3. Use `session_lookup` when the question depends on this conversation's history.
