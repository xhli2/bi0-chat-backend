# Gene Annotation

## Workflow
1. Resolve gene symbols with `bio_mygene_query` (MyGene.info, biothings project).
2. Confirm locus and biotype with `bio_ensembl_gene_lookup` (Ensembl REST).

## Output
Report symbol, Entrez ID, Ensembl gene ID, chromosome coordinates, and biotype when available.
